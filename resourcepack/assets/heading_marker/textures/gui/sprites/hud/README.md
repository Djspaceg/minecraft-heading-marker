# Custom HUD Icons

This directory is intended for custom heading marker icons for future enhancements.

## Current Implementation

The current version uses emoji icons (🔴🔵🟢🟡🟣) displayed via the actionbar, which don't require custom textures.

## Future Enhancement

In a future version, custom PNG textures could be added here to provide:

- **Format:** PNG with transparency
- **Size:** 16x16 pixels (or multiples like 32x32, 64x64 for HD)
- **Color depth:** 32-bit RGBA
- **Naming convention:** Use descriptive names like `marker_red.png`, `marker_blue.png`, etc.

## Placeholder

This directory structure is a placeholder for future custom icon support. The current implementation works without any resource pack textures.
